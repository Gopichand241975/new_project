# requirements.txt
ultralytics>=8.0.0
# Provides YOLOv8 — used in detection.py to detect people in each frame

opencv-python>=4.8.0
# cv2 — used in main.py to open the camera, read frames, save alert snapshots;
# also used inside alerts.py's TamperMonitor to check frame brightness

deep-sort-realtime>=1.3.2
# Provides the DeepSort tracker — used in tracker.py to assign a stable ID
# to each person across frames and measure dwell time at the door

requests>=2.31.0
# Used in alerts.py to call the Telegram Bot API and send the alert
# message + snapshot photo to the homeowner's phone